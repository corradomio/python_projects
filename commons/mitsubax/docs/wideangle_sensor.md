There's no built-in wide-angle/fisheye sensor shipped with Mitsuba 3 — the plugin reference only lists `perspective`, `thinlens`, `orthographic`, `irradiancemeter`, `distant`, `batch`, and `radiancemeter`. To get a wide-angle lens you write a custom Python sensor plugin, following the same `mi.Sensor` subclassing pattern Mitsuba uses for custom BSDFs/emitters. I'll give you a full equidistant (f-θ) fisheye implementation, which is the standard "wide angle" model (supports fields of view up to and beyond 180°, unlike the pinhole `perspective` sensor which is capped below 180°).Let me verify this actually runs correctly rather than just handing over unverified code.It ran, but let me check the output image isn't just blank/black before I trust it.Good — `m_to_world` is provided by the base class. Let me clean up the dead/confusing code I left in and verify with an actual look_at transform and the LLVM variant too.Confirmed working, including on the JIT (`llvm_ad_rgb`) variant and with a FOV beyond 180°. Let me view the render to sanity-check the fisheye distortion visually.That's exactly the expected fisheye look — a circular image (black corners since FOV=200° exceeds a hemisphere, so the circle nearly fills the frame), red floor curving overhead, white floor below, sphere bulging in the center. The implementation is verified working, not just theoretical. Let me clean up the test artifacts and hand you the final file.A few notes on the design:

- **Projection model**: equidistant/f-θ fisheye (`r = f·θ`), which is the standard "wide angle lens" model and, unlike the built-in `perspective` sensor, supports FOV ≥ 180°.
- It subclasses `mi.Sensor` and implements `sample_ray` / `sample_ray_differential`, the same interface the built-in `perspective`/`thinlens` sensors use, so it drops into any scene dict/XML as `'type': 'wideangle'`.
- Aspect-ratio correction keeps the fisheye circle actually circular on non-square films (mirrors how `perspective`'s `fov_axis` works).
- Rays landing outside the image circle get zero throughput rather than wrapping/garbage directions.
- I actually installed Mitsuba and rendered with it (both `scalar_rgb` and the JIT `llvm_ad_rgb` variant, at FOV 170° and 200°) rather than just handing you unverified code — caught and fixed a variant-initialization bug and some dead code in the process. The render above confirms the distortion is correct.

To use it: `mi.set_variant(...)`, then `from wideangle_sensor import WideAngleCamera, register; register()`, then reference `'type': 'wideangle'` with a `fov` in degrees in your scene dict.