One thing worth flagging before I write this: Mitsuba 3 already ships a plugin literally called `orthographic`, but that's a parallel-projection (no-perspective) camera, not a wide-angle lens — it's the opposite end of the spectrum from a fisheye. What you're describing — an "orthographic wide-angle" — is almost certainly the **orthographic fisheye** projection (`r = f·sinθ`), the classic "sine-law" fisheye mapping (as opposed to equidistant `f·θ` or stereographic `2f·tan(θ/2)`, which I gave you earlier). I'll implement that, and name it distinctly so it doesn't collide with the built-in `orthographic` plugin.

One mathematical constraint to flag: `sin(θ)` is only monotonic (invertible) for θ ∈ [0°, 90°], so this projection is capped at **FOV ≤ 180°** — beyond a hemisphere the mapping folds back on itself. I'll enforce that.Now let me actually verify it — including that the FOV cap is enforced correctly.Now the full scene render on the JIT variant, same as before:That's correct — at FOV=180° the image circle fills the whole frame edge-to-edge (as expected, since the whole hemisphere maps to `r=1`), and compared to the equidistant/stereographic renders earlier, the sphere in the center looks noticeably larger relative to frame — exactly the signature center-magnification/edge-compression behavior of the sine-law mapping. Cleaning up test files:Summary of what makes this one different from the equidistant and stereographic versions I gave you earlier:

- **Projection**: `r = f·sin(θ)`, inverted as `θ = asin(r · sin(θ_max))`.
- **Hard FOV cap at 180°**, enforced in `__init__` with a `RuntimeError` (verified above that it actually fires for `fov=200`). This isn't a coding limitation — `sin(θ)` genuinely isn't invertible past 90° off-axis, so there's no valid way to extend this one the way equidistant/stereographic can go past a hemisphere.
- **Visual signature**: strong center magnification and periphery compression — confirmed in the render above, where the sphere reads noticeably larger at the same distance than in the equidistant/stereographic versions.
- Registered as `'orthographic_fisheye'` rather than `'orthographic'`, since Mitsuba's built-in `orthographic` plugin is an unrelated parallel-projection (zero-FOV) camera — I flagged that naming collision up front rather than silently shadowing it.

Same `sample_ray`/`sample_ray_differential` structure and clipping-plane handling as the other two, so it's a drop-in alongside them if you want to switch projection types at the scene-dict level.
