$<varname>      mitsuba
                it is not simple to understand where the variable name terminates
                it is necessary to use a NONTEXT character (a space, ...)

{<varname>}     simple but the string can contain '{}' for other reasons'
${<varname>}    simple, better?
