FbDeepFace.py available in this directory resolve a compatibility problem with
old versions of Tensorflow. The class uses 'LocallyConnected2D' a old TF layer.
To avoid the incompatibility with new versions of TF, the class is implemented
directly inside the file

The ide is to REPLACE the original implementation with the file available HERE

