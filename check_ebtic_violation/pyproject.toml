[project]
name = "check-ebtic-violation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "scipy",
    "torch",
    "torchvision",
    "ultralytics",
    "opencv-python"
]
