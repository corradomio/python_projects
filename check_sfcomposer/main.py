#
# Generatore di set function
# Ci sono diverse possibilita'
#
#   1) set function random
#   2) mobius coefficients random
#
# Le funzioni DEVONO essere sub-modulari
# Un modo alternativo, un po' piu' plausibile, e' COMPORRE la funzione in base a Power Index e Interaction Indices di
# ordine 2,3,... A questo proposito potrebbe essere interessante considerare DUE fattori
#
#   1) un valore random per ogni feature e combinazione di feature
#   2) se le componenti si devono sommare o sottrarre
#
# L'idea e' la seguente: una feature potrebbe collaborare con una seconda ed interferire con una terza.




def main():
    pass



if __name__ == "__main__":
    main()
