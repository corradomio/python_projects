[project]
name = "check-image-contrast"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "path",
    "numpy",
    "matplotlib",
    "opencv-python"
]
